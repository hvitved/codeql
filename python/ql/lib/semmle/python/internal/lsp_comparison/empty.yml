extensions:
  # Make sure that the extensible predicates have at least one definition
  # to avoid errors about undefined extensionals.
  - addsTo:
      pack: codeql/python-all
      extensible: jumpToDef
    data: []
